<%#<%= form_for ([@article, @article.comments.build] ) do |f| %>
<%= form_for ([@article, Comment.new] ) do |f| %>

     <%= f.label :body, 'Enter your comment here' %><br \>
     <%= f.text_area :body, rows: '10', cols: '40'%><br \>
 
     <%=f.submit 'submit' %>

<%end%>